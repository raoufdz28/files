// Check if the JavaScript interface is accessible
if (window.AndroidInterface) {
    // Run a basic command to test
    window.AndroidInterface.executeCommand("ls /");  // Example: List root directory in Android
}